# Requirements for PushWorld PLR training on GPU cluster
# Install with: pip install --user -r requirements-cluster.txt
#
# Note: For JAX with GPU support, you may need to install jax[cuda] separately:
#   pip install --upgrade "jax[cuda12]"
# or match your cluster's CUDA version

# Core JAX stack
jax>=0.4.20
jaxlib>=0.4.20
flax>=0.8.0
chex>=0.1.8
optax>=0.1.7
orbax-checkpoint>=0.5.0
distrax>=0.1.5

# Environments
gymnax>=0.0.6

# Config & logging
hydra-core>=1.3.0
omegaconf>=2.3.0
wandb>=0.16.0

# Utilities
numpy>=1.24.0,<2.0
pillow>=10.0.0
imageio>=2.31.0
tqdm>=4.65.0
